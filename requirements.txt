# =====================================
# 🏆 최고퀸트프로젝트 - 의존성 패키지 (Enhanced Edition)
# =====================================
# 
# 글로벌 퀀트 트레이딩 시스템을 위한 완전체 패키지 목록
# - 🇺🇸 미국 주식 (IBKR 연동)
# - 🇯🇵 일본 주식 (IBKR 연동)  
# - 🪙 암호화폐 (업비트, 바이낸스 연동)
# - 🤖 AI/ML 기반 전략
# - 📊 고급 기술적 분석
# - 🔔 다채널 알림 시스템
# - ☁️ 클라우드 연동
# - 🚀 고성능 최적화
# 
# 설치 방법:
# pip install -r requirements.txt
#
# Author: 최고퀸트팀
# Version: 2.0.0 (Enhanced Edition)
# Project: 최고퀸트프로젝트
# Last Updated: 2025-07-01
# =====================================

# =====================================
# 🐍 Python 버전 요구사항
# =====================================
# Python >= 3.9 (권장: 3.10 또는 3.11)
# 메모리: 최소 8GB RAM (권장: 16GB+)
# 저장공간: 최소 10GB 여유공간

# =====================================
# 📊 핵심 데이터 처리
# =====================================

# 데이터 분석 기초
pandas>=2.1.0,<3.0.0            # 데이터프레임 처리
numpy>=1.24.0,<2.0.0            # 수치 연산
scipy>=1.11.0,<2.0.0            # 과학 계산
pytz>=2023.3                    # 시간대 처리
python-dateutil>=2.8.2          # 날짜 처리
holidays>=0.34                  # 공휴일 처리

# 고성능 데이터 처리
polars>=0.19.0                  # 빠른 데이터프레임 (pandas 대안)
pyarrow>=13.0.0                 # 컬럼형 데이터 처리
fastparquet>=2023.8.0           # Parquet 파일 처리
h5py>=3.9.0                     # HDF5 파일 처리

# =====================================
# 📈 금융 데이터 수집
# =====================================

# 주식 데이터
yfinance>=0.2.20                # 야후 파이낸스
alpha-vantage>=2.3.1            # Alpha Vantage API
quandl>=3.7.0                   # Quandl 데이터
fredapi>=0.5.1                  # FRED 경제 데이터
pandas-datareader>=0.10.0       # 다양한 금융 데이터 소스

# 암호화폐 데이터
ccxt>=4.1.0                     # 암호화폐 거래소 통합 API
pyupbit>=0.2.31                 # 업비트 API
python-binance>=1.0.19          # 바이낸스 API

# 대체 데이터
investpy>=1.0.8                 # Investing.com 데이터
finnhub-python>=2.4.18          # Finnhub API
polygon-api-client>=1.12.0      # Polygon.io API

# =====================================
# 🔗 브로커 연동
# =====================================

# Interactive Brokers
ib-insync>=0.9.86               # IB API 래퍼 (메인)
ibapi>=10.19.2                  # 공식 IB API

# 암호화폐 거래
PyJWT>=2.8.0                    # JWT 토큰 처리
cryptography>=41.0.7            # 암호화

# API 인증
requests-oauthlib>=1.3.1        # OAuth 인증
authlib>=1.2.1                  # 다양한 인증 방식

# =====================================
# 📊 기술적 분석 & 지표
# =====================================

# 기술적 분석 라이브러리
ta>=0.10.2                      # 기술적 지표 (메인)
TA-Lib>=0.4.28                  # 고성능 기술적 분석 (선택사항)
pandas-ta>=0.3.14b              # Pandas 확장 기술적 분석
finta>=1.3                      # 추가 기술적 지표
tulip-python>=0.4.1             # Tulip 지표 (선택사항)

# 고급 분석
pykalman>=0.9.5                 # 칼만 필터
statsmodels>=0.14.0             # 통계 모델
arch>=6.2.0                     # GARCH 모델
pmdarima>=2.0.4                 # ARIMA 모델

# =====================================
# 🤖 머신러닝 & AI
# =====================================

# 핵심 ML 라이브러리
scikit-learn>=1.3.0             # 머신러닝
xgboost>=1.7.6                  # 그래디언트 부스팅
lightgbm>=4.1.0                 # 라이트 GBM
catboost>=1.2                   # 캣부스트

# 딥러닝
torch>=2.0.0                    # PyTorch
tensorflow>=2.13.0              # TensorFlow (선택사항)
transformers>=4.33.0            # Hugging Face 모델

# 특성 엔지니어링
feature-engine>=1.6.0           # 특성 엔지니어링
category-encoders>=2.6.0        # 카테고리 인코딩

# 하이퍼파라미터 튜닝
optuna>=3.4.0                   # 하이퍼파라미터 최적화
hyperopt>=0.2.7                 # 베이지안 최적화
ray[tune]>=2.7.0                # 분산 튜닝 (선택사항)

# 모델 해석
shap>=0.42.1                    # SHAP 값
lime>=0.2.0.1                   # LIME 해석

# MLOps
mlflow>=2.7.0                   # 실험 추적
wandb>=0.15.0                   # Weights & Biases (선택사항)

# =====================================
# 📊 백테스팅 & 포트폴리오
# =====================================

# 백테스팅 프레임워크
backtrader>=1.9.78.123          # 백테스팅 (메인)
zipline-reloaded>=3.0.4         # Zipline 백테스팅
vectorbt>=0.25.2                # 벡터화된 백테스팅
bt>=0.2.9                       # 백테스팅 도구

# 포트폴리오 최적화
PyPortfolioOpt>=1.5.5           # 포트폴리오 최적화
riskfolio-lib>=4.3.0            # 리스크 모델링
empyrical>=0.5.5                # 성과 지표
pyfolio>=0.9.2                  # 포트폴리오 분석 (선택사항)

# 리스크 관리
quantlib-python>=1.31           # 퀀트 라이브러리 (선택사항)

# =====================================
# 🌐 HTTP 요청 & API
# =====================================

# HTTP 클라이언트
aiohttp>=3.8.5,<4.0.0          # 비동기 HTTP
requests>=2.31.0                # 동기 HTTP
httpx>=0.24.1                   # 현대적 HTTP 클라이언트
urllib3>=1.26.16,<3.0.0        # 저수준 HTTP

# WebSocket
websockets>=11.0.3              # WebSocket 클라이언트
websocket-client>=1.6.3         # 단순 WebSocket

# =====================================
# ⚙️ 설정 & 환경
# =====================================

# 설정 관리
PyYAML>=6.0.1                   # YAML 파싱
python-dotenv>=1.0.0            # 환경변수 로드
pydantic>=2.4.0                 # 데이터 검증
hydra-core>=1.3.2               # 설정 관리 (고급)

# 명령행 인터페이스
click>=8.1.7                    # CLI 프레임워크
typer>=0.9.0                    # 현대적 CLI
rich>=13.5.0                    # 터미널 UI

# =====================================
# 📱 알림 시스템
# =====================================

# 텔레그램
python-telegram-bot>=20.5,<21.0 # 텔레그램 봇

# 이메일
emails>=0.6                     # 이메일 발송
smtplib                          # 내장 SMTP (주석으로 표시)

# 슬랙 & 디스코드
slack-sdk>=3.22.0               # 슬랙 API
discord.py>=2.3.2               # 디스코드 봇 (선택사항)

# 푸시 알림
plyer>=2.1.0                    # 크로스 플랫폼 알림 (선택사항)

# =====================================
# 🗃️ 데이터베이스
# =====================================

# ORM & 데이터베이스
sqlalchemy>=2.0.21              # SQL ORM
alembic>=1.12.0                 # 데이터베이스 마이그레이션

# 데이터베이스 드라이버
psycopg2-binary>=2.9.7          # PostgreSQL
pymongo>=4.5.0                  # MongoDB (선택사항)
redis>=5.0.0                    # Redis 캐시

# 경량 데이터베이스
sqlite3                          # 내장 SQLite (주석으로 표시)
duckdb>=0.8.1                   # 임베디드 분석 DB

# =====================================
# 🚀 성능 최적화
# =====================================

# 고속 JSON
orjson>=3.9.7                   # 빠른 JSON 파싱
ujson>=5.8.0                    # 대안 JSON 파서

# 병렬 처리
joblib>=1.3.2                   # 병렬 처리
multiprocessing                  # 내장 멀티프로세싱 (주석으로 표시)
concurrent.futures               # 내장 퓨처스 (주석으로 표시)
asyncio                          # 내장 비동기 (주석으로 표시)

# 프로세스 관리
psutil>=5.9.5                   # 시스템 모니터링

# 메모리 최적화
memory-profiler>=0.61.0         # 메모리 프로파일링
pympler>=0.9                    # 메모리 분석

# =====================================
# 📊 시각화
# =====================================

# 기본 시각화
matplotlib>=3.7.2               # 기본 플롯
seaborn>=0.12.2                 # 통계 시각화
plotly>=5.16.0                  # 인터랙티브 차트

# 고급 시각화
bokeh>=3.2.2                    # 웹 기반 시각화 (선택사항)
altair>=5.1.0                   # 문법적 시각화 (선택사항)
mplfinance>=0.12.10a0           # 금융 차트

# 대시보드
streamlit>=1.26.0               # 웹 대시보드 (선택사항)
dash>=2.14.0                    # Plotly 대시보드 (선택사항)

# =====================================
# 🕰️ 스케줄링 & 태스크
# =====================================

# 스케줄러
APScheduler>=3.10.4             # 고급 스케줄러
schedule>=1.2.0                 # 단순 스케줄러
celery>=5.3.1                   # 분산 태스크 큐 (선택사항)

# 시간 처리
arrow>=1.2.3                    # 현대적 시간 처리
pendulum>=2.1.2                 # 시간대 처리 (대안)

# =====================================
# 🔒 보안 & 암호화
# =====================================

# 암호화
bcrypt>=4.0.1                   # 패스워드 해싱
cryptography>=41.0.7            # 암호화 라이브러리
keyring>=24.2.0                 # 키 저장소

# JWT & 인증
PyJWT>=2.8.0                    # JWT 토큰
passlib>=1.7.4                  # 패스워드 라이브러리

# =====================================
# 🌐 웹 프레임워크 (선택사항)
# =====================================

# API 서버
fastapi>=0.103.0                # FastAPI (권장)
uvicorn>=0.23.2                 # ASGI 서버
gunicorn>=21.2.0                # WSGI 서버

# 전통적 웹 프레임워크
flask>=2.3.3                    # Flask (가벼운 옵션)

# WebSocket 서버
fastapi-websocket-pubsub>=0.3.0 # WebSocket PubSub

# =====================================
# 🧪 테스트 & 품질
# =====================================

# 테스트 프레임워크
pytest>=7.4.2                   # 테스트 프레임워크
pytest-asyncio>=0.21.1          # 비동기 테스트
pytest-cov>=4.1.0               # 커버리지 측정
pytest-mock>=3.11.1             # 모킹
pytest-xdist>=3.3.1             # 병렬 테스트

# 코드 품질
black>=23.7.0                   # 코드 포매터
isort>=5.12.0                   # import 정렬
flake8>=6.1.0                   # 린터
pylint>=2.17.5                  # 린터 (고급)

# 타입 체킹
mypy>=1.5.1                     # 타입 체커
types-requests>=2.31.0.2        # requests 타입 스텁
types-PyYAML>=6.0.12.11         # PyYAML 타입 스텁

# 보안 스캔
bandit>=1.7.5                   # 보안 린터
safety>=2.3.5                   # 의존성 보안 스캔

# =====================================
# 📝 문서화
# =====================================

# 문서 생성
sphinx>=7.2.6                   # 문서 생성 (선택사항)
mkdocs>=1.5.3                   # 마크다운 문서 (선택사항)

# API 문서
sphinx-autodoc-typehints>=1.24.0 # 타입 힌트 문서 (선택사항)

# =====================================
# 🛠️ 유틸리티
# =====================================

# 진행바 & UI
tqdm>=4.66.1                    # 진행바
colorama>=0.4.6                 # 터미널 컬러
termcolor>=2.3.0                # 터미널 컬러 (대안)

# 파일 처리
pathlib                          # 내장 경로 처리 (주석으로 표시)
glob                             # 내장 파일 패턴 (주석으로 표시)
shutil                           # 내장 파일 유틸 (주석으로 표시)

# 문자열 처리
regex>=2023.8.8                 # 정규표현식 (확장)
fuzzywuzzy>=0.18.0              # 퍼지 문자열 매칭

# =====================================
# 🌍 다국가화 & 지역화
# =====================================

# 다국가 지원
babel>=2.12.1                   # 국제화 (선택사항)
phonenumbers>=8.13.19           # 전화번호 처리 (선택사항)

# =====================================
# ☁️ 클라우드 & 외부 서비스
# =====================================

# AWS
boto3>=1.28.57                  # AWS SDK (선택사항)
botocore>=1.31.57               # AWS 코어 (선택사항)

# Google Cloud
google-cloud-storage>=2.10.0    # GCS (선택사항)

# Azure
azure-storage-blob>=12.17.0     # Azure Blob (선택사항)

# 파일 압축
zipfile                          # 내장 ZIP (주석으로 표시)
gzip                             # 내장 GZIP (주석으로 표시)

# =====================================
# 🔄 워크플로우 & 자동화
# =====================================

# 태스크 자동화
invoke>=2.2.0                   # 태스크 러너 (선택사항)
fabric>=3.2.2                   # 배포 자동화 (선택사항)

# =====================================
# 🖥️ 시스템별 의존성
# =====================================

# Windows 전용
pywin32>=306; sys_platform == "win32"                    # Windows API
winsound; sys_platform == "win32"                        # Windows 사운드

# macOS 전용  
pyobjc-core>=9.2; sys_platform == "darwin"              # macOS API
pyobjc-framework-Cocoa>=9.2; sys_platform == "darwin"   # Cocoa 프레임워크

# Linux 전용
python-dbus>=1.3.2; sys_platform == "linux"            # D-Bus (선택사항)

# =====================================
# 🔄 실시간 데이터 처리
# =====================================

# 스트리밍 데이터
kafka-python>=2.0.2             # Apache Kafka (선택사항)
confluent-kafka>=2.2.0          # Confluent Kafka (선택사항)

# 메시지 큐
pika>=1.3.2                     # RabbitMQ (선택사항)
kombu>=5.3.2                    # 메시지 큐 추상화 (선택사항)

# =====================================
# 📊 고급 분석 도구
# =====================================

# 시계열 분석
tslearn>=0.6.2                  # 시계열 머신러닝 (선택사항)
tsfresh>=0.20.1                 # 시계열 특성 추출 (선택사항)
sktime>=0.22.0                  # 시계열 분석 (선택사항)

# 베이지안 분석
pymc>=5.7.2                     # 베이지안 모델링 (선택사항)
arviz>=0.16.1                   # 베이지안 분석 시각화 (선택사항)

# =====================================
# 🎯 트레이딩 특화 도구
# =====================================

# 옵션 분석
py_vollib>=1.0.1                # 옵션 변동성 (선택사항)
QuantLib>=1.31                  # 퀀트 라이브러리 (선택사항)

# 채권 분석
bond-pricing>=0.1.0             # 채권 가격 계산 (선택사항, 존재하지 않을 수 있음)

# =====================================
# 📋 개발 도구
# =====================================

# 코드 분석
vulture>=2.10                   # 데드 코드 찾기 (선택사항)
radon>=6.0.1                    # 복잡도 분석 (선택사항)

# Git 훅
pre-commit>=3.4.0               # Git pre-commit 훅 (선택사항)

# =====================================
# 🚀 프로덕션 배포 (고급)
# =====================================

# 프로세스 관리
supervisor>=4.2.5               # 프로세스 감독 (선택사항)

# 로깅
loguru>=0.7.2                   # 현대적 로깅
structlog>=23.1.0               # 구조화된 로깅 (선택사항)

# 모니터링
prometheus-client>=0.17.1       # Prometheus 메트릭 (선택사항)
statsd>=4.0.1                   # StatsD 메트릭 (선택사항)

# 헬스체크
py-healthcheck>=1.10.1          # 헬스체크 엔드포인트 (선택사항)

# =====================================
# 📚 Jupyter & 노트북 (개발용)
# =====================================

# Jupyter 환경
jupyter>=1.0.0                  # Jupyter 메타패키지 (선택사항)
jupyterlab>=4.0.6               # JupyterLab (선택사항)
notebook>=7.0.3                 # Jupyter Notebook (선택사항)
ipykernel>=6.25.2               # IPython 커널 (선택사항)

# Jupyter 확장
ipywidgets>=8.1.1               # 인터랙티브 위젯 (선택사항)
plotly-widget>=0.4.0            # Plotly 위젯 (선택사항)

# =====================================
# 🎮 게임화 & 시뮬레이션 (향후 확장)
# =====================================

# 시뮬레이션
simpy>=4.0.2                    # 이산 이벤트 시뮬레이션 (선택사항)

# =====================================
# 🧬 실험적 패키지 (선택사항)
# =====================================

# 그래프 분석
networkx>=3.1                   # 네트워크 분석 (선택사항)

# 자연어 처리 (센티멘트 분석 등)
nltk>=3.8.1                     # 자연어 툴킷 (선택사항)
spacy>=3.7.2                    # 현대적 NLP (선택사항)
textblob>=0.17.1                # 간단한 NLP (선택사항)

# 이미지 처리 (차트 분석 등)
pillow>=10.0.0                  # 이미지 처리 (선택사항)
opencv-python>=4.8.1.78         # 컴퓨터 비전 (선택사항)

# =====================================
# 🔧 패키지 버전 호환성 해결
# =====================================

# 버전 충돌 해결을 위한 명시적 의존성
setuptools>=68.2.2              # 패키지 도구
wheel>=0.41.2                   # 휠 패키지
pip>=23.2.1                     # 패키지 설치 도구

# =====================================
# 📖 설치 및 사용 가이드
# =====================================

# 설치 순서 (권장):
# 1. 기본 환경 설정:
#    python -m venv venv
#    source venv/bin/activate  # Linux/Mac
#    venv\Scripts\activate     # Windows
#
# 2. 기본 패키지 업그레이드:
#    pip install --upgrade pip setuptools wheel
#
# 3. 핵심 패키지 설치:
#    pip install pandas numpy yfinance ta pyupbit ib-insync
#
# 4. 전체 패키지 설치:
#    pip install -r requirements.txt
#
# 5. 선택적 패키지 설치 (필요시):
#    pip install tensorflow torch mlflow
#
# 트러블슈팅:
# - TA-Lib 설치 오류: conda install -c conda-forge ta-lib
# - M1 Mac에서 일부 패키지 오류: pip install --no-use-pep517 <package>
# - Windows에서 일부 패키지 오류: Microsoft C++ Build Tools 설치
#
# 메모리 요구사항:
# - 최소: 8GB RAM
# - 권장: 16GB+ RAM (ML 모델 포함시)
# - 스토리지: 10GB+ 여유공간
#
# 성능 최적화:
# - SSD 사용 권장
# - 멀티코어 CPU 권장
# - GPU 사용시 CUDA 설치 (PyTorch/TensorFlow)

# =====================================
# ⚠️ 중요 주의사항
# =====================================

# 1. 가상환경 사용 필수:
#    - 시스템 Python과 분리
#    - 의존성 충돌 방지
#    - 프로젝트별 독립성 보장
#
# 2. 정기적인 업데이트:
#    - pip list --outdated 로 확인
#    - 보안 패치 적용
#    - 호환성 테스트 필수
#
# 3. 라이센스 확인:
#    - 상업적 사용시 라이센스 검토
#    - GPL 패키지 주의
#    - 기업 환경에서 승인된 패키지만 사용
#
# 4. 보안 고려사항:
#    - pip audit로 취약점 스캔
#    - 신뢰할 수 있는 소스에서만 설치
#    - requirements.txt 정기 검토

# =====================================
# 🎯 패키지 분류별 우선순위
# =====================================

# 🔥 필수 (Core):
# pandas, numpy, yfinance, ta, pyupbit, ib-insync
# requests, python-dotenv, python-telegram-bot

# ⭐ 권장 (Recommended):
# scikit-learn, plotly, fastapi, pytest, black
# sqlalchemy, redis, APScheduler

# 💡 선택사항 (Optional):
# tensorflow, torch, mlflow, streamlit, jupyter
# 클라우드 서비스, 고급 시각화, 실험적 기능

# 🚫 주의 (Caution):
# TA-Lib (컴파일 필요), QuantLib (복잡한 설치)
# 대용량 ML 라이브러리들 (메모리 사용량 높음)

# =====================================
# ✅ 설치 검증 체크리스트
# =====================================

# □ 가상환경이 활성화되어 있는가?
# □ Python 버전이 3.9 이상인가?
# □ 기본 패키지들이 정상 설치되었는가?
# □ API 키 설정이 완료되었는가?
# □ 데이터베이스 연결이 가능한가?
# □ 테스트 스크립트가 정상 실행되는가?
# □ 메모리 사용량이 적정한가?
# □ 로그 파일이 정상 생성되는가?

# =====================================
# 🏁 최고퀸트프로젝트 - 패키지 완성!
# =====================================
